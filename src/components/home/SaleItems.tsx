import React from "react";
import { View, Text } from "react-native";
import { MainProps } from "../../types/types";

export default function SaleItems({ productInfo }: MainProps) {
  return (
    <View>
      <Text>SaleItems</Text>
    </View>
  );
}
