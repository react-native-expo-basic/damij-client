import React from "react";
import { View, Text } from "react-native";
import { MainProps } from "../../types/types";

export default function NewItems({ productInfo }: MainProps) {
  return (
    <View>
      <Text>NewItems</Text>
    </View>
  );
}
